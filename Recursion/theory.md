Recursion- when a function calls itself recursively(iteratively)

Every recursive function needs 2 parts:

Base Case — the condition where the function STOPS calling itself (otherwise it will run forever and crash — "Stack Overflow").
Recursive Case — where the function calls itself with a smaller/simpler input, moving it closer to the base case.
